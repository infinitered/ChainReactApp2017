import { ViewStyle } from 'react-native'

interface SocialMediaButtonStylesType {
  left: ViewStyle
  right: ViewStyle
}
const SocialMediaButtonStyles: SocialMediaButtonStylesType = {
  left: { marginLeft: 30 },
  right: { marginRight: 30 }
}

export default SocialMediaButtonStyles
